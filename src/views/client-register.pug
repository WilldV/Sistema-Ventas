<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        link(rel="stylesheet", href="https://bootswatch.com/4/cerulean/bootstrap.min.css")
        title Sistema de ventas
    body(class="container-fluid")
        header(class="navbar navbar-expand-lg navbar-dark bg-primary")
            a(href="/" class="navbar-brand") Inicio
        main
            div(class="jumbotron ")
                div(class="row justify-content-center ")    
                    div(class="col-12 col-lg-6 border border-primary")
                        h1(class = "text-center mb-3") Registro de cliente
                        form(action="/clients/add" method="POST")
                            div(class="form-group")
                                label(for="name") Nombre:
                                input(type="name" name="name" class="form-control" required)
                            div(class="form-group")
                                label(for="name") Apellido:
                                input(type="surname" name="surname" class="form-control" required)
                            div(class="form-group")
                                label(for="name") Dirección:
                                input(type="Adress" name="adress" class="form-control")
                            div(class="form-group")
                                label(for="name") Teléfono:
                                input(type="phone" name="phone" class="form-control" required)
                            div(class="form-group")
                                input(type="submit" class = "btn btn-success")