<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        link(rel="stylesheet", href="https://bootswatch.com/4/cerulean/bootstrap.min.css")
        title Sistema de ventas
    body(class="container-fluid")
        header(class="navbar navbar-expand-lg navbar-dark bg-primary")
            a(href="/" class="navbar-brand") Inicio
        main
             div(class="jumbotron justify-content-center")
                h1(class = "text-center mb-3") Clientes registrados
                div(class="row justify-content-around") 
                    each val in data
                        div(class="card border-primary col-12 col-sm-6 col-md-4 col-lg-3")
                            div(class="card-header text-center")= val.name+' '+val.surname
                            div(class="card-body")
                                h4(class="card-title")= 'ID: '+val.code
                                p= 'Dirección: '+val.adress
                                p= 'Teléfono: '+val.phone
                                form(action="/clients/"+val.code+"" method = 'post')
                                    div(class="form-group")
                                        h6.card-title Consultar compras por mes:
                                        input(type="number" name="month" min="1" max="12" class="form-control" required)
                                        input(type="submit" value="Consultar")
                                a(href="/clients/"+val.code+"/purchase" class="btn btn-success mb-2") Realizar una compra