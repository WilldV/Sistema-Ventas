<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        link(rel="stylesheet", href="https://bootswatch.com/4/cerulean/bootstrap.min.css")
        title Sistema de ventas
    body(class="container-fluid")
        header(class="navbar navbar-expand-lg navbar-dark bg-primary")
            a(href="/" class="navbar-brand") Inicio
        main
             div(class="jumbotron justify-content-center")
                h1(class = "text-center mb-3") Proveedores registrados
                div(class="row justify-content-around") 
                    each val in providers
                        div(class="card border-primary col-12 col-sm-6 col-md-4 col-lg-3")
                            div(class="card-header text-center")= val.name + " "+ val.surname
                            div(class="card-body")
                                h4(class="card-title")= 'ID: '+val.code
                                p= 'Dirección: '+val.adress
                                p= 'Provincia: '+val.province
                                p= 'Teléfono: '+val.phone
                                form(action="/providers/"+val.code+"/add" method='post')
                                    div(class="form-group")
                                        label(for="name") Agregar provisión:
                                        select(class="form-control" name="productcode")
                                            each value in products
                                                option(value=""+value.code)= value.description
                                    div(class="form-group")
                                        input(type="submit" class="btn btn-success")
                                div(class="row justify-content-center")  
                                    a(href="/providers/"+val.code class="btn btn-primary col-5") Consultar provisiones
                                    a(href="/providers/"+val.code+"/delete" class="btn btn-warning col-5") Eliminar proveedor